name: DART iOS (built from ArcLake-iOS)
# This repo's builds are triggered from DART-Skyboard/ArcLake-iOS
# which has the required signing secrets.
# See: .github/workflows/dart-testflight.yml in ArcLake-iOS
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trigger build from ArcLake-iOS repo dart-testflight workflow"
